# Strategy Specs

Put reviewed, implementation-ready strategy specs here.

Use `docs/templates/strategy_spec_template.md` for each spec.

Implementation must not start from a loose candidate note alone. Fast mode may use a one-page spec, but it still requires signal, execution, risk, state, and validation checks.
